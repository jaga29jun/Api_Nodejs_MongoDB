# API using NodeJs MongoDB #

API based requests are created using NodeJS and MongoDB.

#Steps:#

- Install express, bodyparser, moment-timezone, mongoDB to use this application.
- Configure MongoDB to 27017 port.
- After installing and configuring the code. Use ```node index.js``` to run the project.

API Methods and URL are included in ````index.js```
